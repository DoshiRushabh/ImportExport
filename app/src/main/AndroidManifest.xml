<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rushabh.importandexport">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ModeOfTransActivity" />
        <activity android:name=".activities.ForignTradeActivity" />
        <activity android:name=".activities.IncotermsActivity" />
        <activity android:name=".activities.PaymentTermsActivity" />
        <activity android:name=".activities.IdentifyingBuyersPaymentActivity" />
        <activity android:name=".activities.InternationalBodiesActivity" />
        <activity android:name=".activities.ImportExportCycleSctivity" />
        <activity android:name=".activities.InsuAndRiskActivity" />
        <activity android:name=".activities.GovermentBenefitsActivity" />
        <activity android:name=".activities.ImportExportBasicsActivity" />
        <activity android:name=".activities.LcChecklistActivity" />
        <activity android:name=".activities.ProductMarcetSelectActivity" />
        <activity android:name=".activities.ImportDocumentActivity" />
        <activity android:name=".activities.AboutUsActivity" />
    </application>

</manifest>